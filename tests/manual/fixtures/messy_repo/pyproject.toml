[project]
name = "messy"
version = "0.1.0"
description = "Retrofit fixture: a repo the team wrote before any tooling existed."
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/messy"]

[tool.uv]
package = true
