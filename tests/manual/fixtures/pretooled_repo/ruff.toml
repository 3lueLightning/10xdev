# Hand-written by the team before quality-gates existed. Deliberately
# different from the skill's own ruff.toml defaults (line-length, target) so a
# scenario run shows whether `apply.py` preserves or clobbers it.
line-length = 100
target-version = "py311"

[lint]
select = ["E", "F"]
