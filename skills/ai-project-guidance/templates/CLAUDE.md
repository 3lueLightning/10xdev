# See AGENTS.md

This project uses `AGENTS.md` as the single canonical guidance file for AI
coding assistants. All rules and conventions live there.

This file exists only as a pointer for tools that look specifically for
`CLAUDE.md`. Do not duplicate guidance here — keep one source of truth so the
two never drift apart.
