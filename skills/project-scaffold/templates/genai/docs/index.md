# {{project_name}}

A Python GenAI project.

Start here:

- **How the project is organised** — see the repository `README.md`; every
  folder also carries its own `README.md`.
- **Conventions and the reasons behind them** — `AGENTS.md` (written for AI
  assistants, worth a human read too).
- **[API reference](reference.md)** — generated from the package docstrings.

To work on these docs: `task docs-serve` and edit the files under `docs/`.
