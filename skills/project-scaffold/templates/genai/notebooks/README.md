# `notebooks/` — experimentation sandbox

The most relaxed area: no lint, typing, size, or dependency-hygiene checks — only
secret scanning (output cells included). Outputs are kept so results stay
visible. Import the package by name (`import {{pkg}}`); see
`00_getting_started.ipynb` for the `%autoreload` + editable-install setup.
